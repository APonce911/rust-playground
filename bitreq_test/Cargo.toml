[package]
name = "bitreq_test"
version = "0.1.0"
edition = "2024"

[dependencies]
bitreq = { version = "0.3.1", default-features = false }
tokio = { version = "1", features = ["rt", "macros"] }

[features]
rustls = ["bitreq/async-https-rustls"]
native-tls = ["bitreq/async-https-native-tls"]
